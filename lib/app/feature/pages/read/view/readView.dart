import 'package:flutter/material.dart';

class ReadView extends StatelessWidget {
  const ReadView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Read Screen"),),
    );
  }
}