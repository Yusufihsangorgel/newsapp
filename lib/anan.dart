import 'package:flutter/material.dart';

class Anan extends StatelessWidget {
  const Anan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("anan"),
    );
  }
}